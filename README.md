# ScheduleGenerator

**Веб‑приложение для автоматического построения учебного расписания**

> Лёгкий, надёжный и удобный инструмент для школ, колледжей и вузов. Автоматически распределяет пары с учётом ограничений преподавателей, равномерно распределяет нагрузку и позволяет гибко редактировать результат.

---

## Ключевые возможности

* ✅ Управление преподавателями и предметами (ФИО, email, часы, приоритеты и цветовая маркировка)
* 🤖 Автоматическая генерация расписания (неделя/день) с учётом негативных фильтров
* 🚫 Ограничения для преподавателей (запрещённые дни/пары)
* ✏️ Ручное редактирование слотов: правка, блокировка, удаление
* 🔄 Автообновление оставшихся часов и учёт ручных правок (они не перезаписываются)
* 🧠 Алгоритм равномерно распределяет нагрузку и случайно выбирает слоты для разнообразия
* ⚡ Быстрая и лёгкая архитектура: FastAPI + aiosqlite + чистый HTML/CSS/JS

---

## Скриншоты


---

## Быстрый старт

**Требования**:

* Python 3.8 или выше

**Проверка установки Python**:

```bash
python --version
# должно показывать Python 3.8+
```

**Первый запуск (один раз)**

1. Распакуйте репозиторий и откройте папку `ScheduleGenerator/`.
2. Запустите `install.bat` (двойной клик) — скрипт установит все зависимости. Файл запускается ТОЛЬКО первый раз.
3. После сообщения `INSTALLATION COMPLETE!` запустите `start.bat`.

`start.bat` автоматически поднимет сервер и откроет браузер с приложением. В дальнейшем, единственный файл, который нужно будет запускать для полной работоспособности приложения.

---

## Режимы запуска

### Режим разработки (рекомендуется)

**`start.bat`**

* Запускает сервер с авто‑перезагрузкой (hot reload).
* Открывает браузер автоматически.
* Удобно для разработки и правок в коде.

### Только сервер (production-like)

**`server.bat`**

* Запускает сервер без авто‑перезагрузки.
* Не открывает браузер.
* Подходит для демонстрации и запуска на сервере.

### Только открыть браузер

**`browser.bat`**

* Проверяет, запущен ли сервер и открывает браузер.
* Не запускает сервер.

---

## Адреса приложения

* Основное приложение: `http://127.0.0.1:8000`
* Проверка здоровья: `http://127.0.0.1:8000/health`

> Если вы меняете порт, обновите соответствующие `.bat` файлы (параметр `--port 8000`).

---

## Установка вручную (если нужен pip)

```bash
python -m venv .venv
.\.venv\Scripts\activate    # Windows
pip install -r requirements.txt
```

---

## Частые проблемы и их решение

**Ошибка: `Python not found`**

* Переустановите Python и обязательно отметьте **Add Python to PATH** при установке.

**Порт 8000 занят**

* Откройте `server.bat` или `start.bat` и замените `--port 8000` на свободный порт (например, `--port 8080`).

**Зависимости не установились**

* Запустите `install.bat` ещё раз или выполните ручную установку через `pip install -r requirements.txt`.

**Остановка сервера**

* В окне сервера нажмите `Ctrl+C`.

---

## API (основные endpoints)

> Все эндпоинты находятся под префиксом `/api`

### Предметы

* `GET /api/subjects` — список предметов
* `POST /api/subjects` — добавить предмет
* `PUT /api/subjects/{id}` — редактировать предмет
* `DELETE /api/subjects/{id}` — удалить предмет

### Расписание

* `GET /api/schedule?week_start=YYYY-MM-DD` — получить расписание на неделю
* `POST /api/schedule/generate` — сгенерировать неделю
* `POST /api/schedule/generate_day` — сгенерировать день

### Слоты

* `PUT /api/slot/{date}/{slot}` — обновить слот
* `DELETE /api/slot/{date}/{slot}` — очистить слот
* `POST /api/slot/{date}/{slot}/block` — заблокировать слот

### Преподаватели

* `GET /api/teachers` — список преподавателей
* `POST /api/teachers` — добавить преподавателя
* `PUT /api/teachers/{id}` — редактировать преподавателя

> Для работы с API удобно использовать Postman или curl. Примеры запросов можно добавить в папку `/docs`.

---

## Структура базы данных (основные таблицы)

* `teachers` — данные преподавателей (id, ФИО, email, заметки)
* `subjects` — предметы (id, название, teacher_id, hours_total, priority, color)
* `lessons` — фактические пары/слоты
* `blocked_slots` — заблокированные слоты
* `settings` — настройки приложения
* `negative_filters` — ограничения для преподавателей

---

## Советы по эксплуатации

* Для долгой работы на Windows используйте `server.bat` без `pause` в конце — окно закроется автоматически.
* Для демонстрации на другом компьютере убедитесь, что сервер доступен в сети и укажите корректный хост/порт.
* Резервируйте базу `instance/store.db` перед крупными правками.

---

## Для разработчиков

* Backend: FastAPI + aiosqlite — асинхронная модель повышает отзывчивость.
* Frontend: чистый HTML/CSS/JS + Jinja2 шаблоны.
* Рекомендуется оформить тесты для основных API (см. папку `tests`).

Запуск тестов (пример):

```bash
pytest -q
```

---

## Вклад и обратная связь

1. Форкните репозиторий
2. Создайте ветку `feature/your-feature`
3. Сделайте изменения и добавьте тесты
4. Откройте Pull Request с описанием изменений

Если нужно — оставьте issue с описанием бага или пожеланием.

---

